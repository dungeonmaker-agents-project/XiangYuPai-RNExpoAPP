# 向娱拍前端 React Native App 模块架构总览

## 📌 平台简介

向娱拍是一个基于 React Native + Expo 的**服务交易撮合平台移动端应用**，连接服务提供者（陪练/教练）与客户，提供线上游戏陪玩、线下本地服务（台球、健身等）的预订交易，并附带社交互动功能。

### 核心商业模式

**平台定位**：服务交易平台 + 社交互动 + 门店合作

**业务模式**（优先级从高到低）：
1. **线下陪玩服务**（核心业务）：
   - 台球陪练、桌游陪玩、剧本杀等高客单价项目
   - 与线下门店深度合作（台球厅、桌游吧、剧本杀店等）
2. **线上游戏陪玩**（补充业务）：
   - 王者荣耀、英雄联盟等游戏陪玩
3. **社交互动**（用户粘性）：
   - 动态发布、点赞评论、关注互动
4. **组局活动**（线下引流）：
   - 用户自发组织线下活动

## 🏗️ 技术架构

### 核心技术栈

```yaml
基础框架:
  - React Native: 0.72+
  - Expo SDK: 最新版
  - Expo Router: 文件式路由

开发语言:
  - TypeScript: 严格模式

状态管理:
  - Zustand: 轻量级状态管理
  - React Context: 局部状态共享

网络通信:
  - Axios: HTTP 客户端
  - WebSocket: 实时消息

数据存储:
  - Expo SecureStore: 敏感数据加密存储
  - AsyncStorage: 一般数据持久化

UI 组件:
  - React Native 原生组件
  - 自定义组件库

开发工具:
  - ESLint: 代码规范
  - Prettier: 代码格式化
```

### 三层架构体系

```
前端项目架构体系（三层递进）
│
├─ 🏢 项目层 - Features 模块化架构
│   ├─ 目的：组织大型业务功能模块
│   ├─ 范围：整个项目的顶层结构
│   ├─ 示例：AuthModule, Homepage, Messages, Profile
│   └─ 组织：src/features/{ModuleName}/
│
├─ 📄 页面层 - 伪页面组件架构
│   ├─ 目的：组织页面级功能组件
│   ├─ 范围：单个页面内的组件组织
│   ├─ 示例：MainPage, DetailPage, UserCard, SearchBar
│   ├─ 组织：src/features/{ModuleName}/{PageName}/{AreaName}/
│   └─ 模式：扁平化（简单组件）/ 嵌套化（复杂组件）
│
└─ 🧩 模块层 - 八段式单文件架构
    ├─ 目的：组织单个组件/页面的内部代码结构
    ├─ 范围：单文件内的代码组织
    ├─ 组织：按 9 个 region 职责分离
    └─ 层级：Banner → Imports → Types → Constants → Utils → State → Logic → UI → Export
```

## 📊 功能模块总览

### 模块分布图

```
┌─────────────────────────────────────────────────────────────────┐
│                      XiangYuPai-RNExpoAPP                       │
│                    向娱拍 React Native 前端应用                    │
└─────────────────────────────┬───────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          │                   │                   │
          ↓                   ↓                   ↓
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  🔐 AuthModule  │  │  🏠 Homepage    │  │  🔍 Discovery   │
│  认证模块        │  │  首页模块        │  │  发现模块        │
│                 │  │                 │  │                 │
│ • 密码/验证码登录 │  │ • 用户列表瀑布流  │  │ • 三Tab动态流    │
│ • 忘记密码      │  │ • 多维度筛选     │  │ • 瀑布流列表     │
│ • 自动登录      │  │ • 组局中心入口   │  │ • 互动功能      │
│ • 路由守卫      │  │ • 搜索功能      │  │ • 话题系统      │
└─────────────────┘  └─────────────────┘  └─────────────────┘
          │                   │                   │
          ↓                   ↓                   ↓
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  💬 Messages    │  │  👤 Profile     │  │  🎉 Activity    │
│  消息模块        │  │  个人中心模块    │  │  组局活动模块    │
│                 │  │                 │  │                 │
│ • 私信聊天      │  │ • 四Tab个人主页  │  │ • 活动列表      │
│ • 点赞/评论通知  │  │ • 资料编辑      │  │ • 活动详情      │
│ • 新粉丝通知    │  │ • 订单管理      │  │ • 发布活动      │
│ • WebSocket实时 │  │ • 系统设置      │  │ • 活动筛选      │
└─────────────────┘  └─────────────────┘  └─────────────────┘
```

### 模块详情表

| 模块 | 核心功能 | 页面数 | 技术特点 | 后端对接 |
|------|---------|-------|---------|---------|
| **AuthModule** | 用户认证 | 3 | 多Store分离、SecureStore | xypai-auth |
| **Homepage** | 首页浏览 | 8+ | Flow模式、SharedComponents | xypai-app-bff |
| **Discovery** | 内容发现 | 3 | 瀑布流、乐观更新 | xypai-content |
| **Messages** | 消息通知 | 6 | WebSocket、实时聊天 | xypai-chat |
| **Profile** | 个人中心 | 15+ | 深度嵌套、四Tab | xypai-user |
| **Activity** | 组局活动 | 4 | 简洁扁平、筛选系统 | xypai-order |

## 📁 项目目录结构

```
XiangYuPai-RNExpoAPP/
├── app/                          # Expo Router 路由目录
│   ├── (tabs)/                   # Tab 导航页面
│   │   ├── homepage/             # 首页 Tab
│   │   ├── discover/             # 发现 Tab
│   │   ├── messages/             # 消息 Tab
│   │   └── profile/              # 我的 Tab
│   ├── auth/                     # 认证相关页面
│   ├── feed/                     # 动态详情页面
│   ├── user/                     # 用户详情页面
│   └── _layout.tsx               # 根布局
│
├── src/
│   ├── features/                 # 📦 业务功能模块（核心）
│   │   ├── AuthModule/           # 🔐 认证模块
│   │   ├── Homepage/             # 🏠 首页模块
│   │   ├── Discovery/            # 🔍 发现模块
│   │   ├── Messages/             # 💬 消息模块
│   │   ├── Profile/              # 👤 个人中心模块
│   │   └── Activity/             # 🎉 组局活动模块
│   │
│   └── [其他全局目录...]
│
├── components/                   # 全局共享组件
├── hooks/                        # 全局 Hooks
├── stores/                       # 全局状态管理
├── services/                     # 全局服务
│   └── api/                      # API 服务层
├── constants/                    # 全局常量
├── assets/                       # 静态资源
└── scripts/                      # 构建脚本
```

## 🔑 各模块详解

### 1. AuthModule 认证模块

**核心定位**：用户认证授权，提供登录、注册、密码管理功能

**目录结构**：
```
AuthModule/
├── api/authApi.ts                    # 认证 API 服务
├── config/                           # 配置（路由白名单、测试账号）
├── data/countries.ts                 # 国家地区数据
├── hooks/                            # 认证 Hooks
│   ├── useAuthInitialization.ts      # 认证初始化
│   └── useRouteGuard.ts              # 路由守卫
├── stores/                           # 认证状态（4个Store）
│   ├── authStore.ts                  # 主状态（Token/用户）
│   ├── authFlowStore.ts              # 流程状态
│   ├── authDataStore.ts              # 数据状态
│   └── authUIStore.ts                # UI状态
├── LoginMainPage/                    # 登录主页面
│   ├── PhoneInputArea/               # 手机号输入
│   ├── PasswordInputArea/            # 密码输入
│   ├── CodeInputArea/                # 验证码输入
│   └── ActionButtonArea/             # 登录按钮
├── ForgotPasswordPage/               # 忘记密码
├── ResetPasswordPage/                # 重置密码
└── SharedComponents/                 # 共享组件
```

**技术亮点**：
- 多 Store 分离设计（主状态/流程/数据/UI）
- Expo SecureStore 加密存储 Token
- 路由守卫自动保护页面
- 支持多地区手机号验证

---

### 2. Homepage 首页模块

**核心定位**：用户列表浏览、服务筛选、组局入口

**目录结构**：
```
Homepage/
├── index.ts                          # 模块统一导出
├── types.ts                          # 模块级类型
├── MainPage/                         # 首页主页面
│   ├── HeaderArea/                   # 顶部导航
│   ├── GameBannerArea/               # 游戏横幅
│   ├── FunctionGridArea/             # 功能入口网格
│   ├── FilterTabsArea/               # 筛选标签栏（嵌套）
│   │   ├── RegionSelector/           # 地区选择器
│   │   └── FilterSelector/           # 筛选器
│   └── UserListArea/                 # 用户列表（嵌套）
│       └── UserCardComponent/        # 用户卡片
├── SharedComponents/                 # 模块共享组件
│   └── BottomSheets/                 # 底部弹窗
├── EventFlow/                        # 组局中心功能流
├── SearchFlow/                       # 搜索功能流
├── FilterFlow/                       # 筛选功能流
├── ServiceFlow/                      # 服务详情功能流
├── LocationFlow/                     # 位置选择功能流
└── UserDetailFlow/                   # 用户详情功能流
```

**技术亮点**：
- Flow 模式按业务流程组织子模块
- SharedComponents 统一管理共享组件
- 嵌套化复杂组件（FilterTabsArea、UserListArea）
- 状态 Hooks 分离（State/Data/Navigation）

---

### 3. Discovery 发现模块

**核心定位**：社交内容发现，动态流浏览、互动功能

**目录结构**：
```
Discovery/
├── index.tsx                         # 模块入口
├── types.ts                          # 类型定义（Feed/Comment/Topic）
├── constants.ts                      # 常量配置
├── MainPage/                         # 发现主页面（三Tab）
│   ├── NavigationArea/               # Tab导航
│   ├── ContentArea/                  # 内容列表
│   │   └── WaterfallList.tsx         # 瀑布流列表
│   └── FeedCard/                     # 动态卡片
├── DetailPage/                       # 动态详情页
│   ├── ShareModal.tsx                # 分享弹窗
│   └── ReportModal.tsx               # 举报弹窗
├── TopicDetailPage/                  # 话题详情页
└── utils/dataTransform.ts            # 数据转换工具
```

**技术亮点**：
- 三 Tab 独立数据（关注/热门/同城）
- 瀑布流双列布局
- 乐观更新（点赞/收藏立即反馈）
- 数据转换层解耦前后端

---

### 4. Messages 消息模块

**核心定位**：消息通知中心，私信聊天、互动通知

**目录结构**：
```
Messages/
├── index.ts                          # 模块统一导出
├── types.ts                          # 模块级类型
├── constants.ts                      # 模块级常量
├── MainPage/                         # 消息主页面
│   ├── CategoryArea/                 # 四宫格分类
│   ├── ConversationArea/             # 对话列表
│   └── NavigationArea/               # 导航栏
├── ChatPage/                         # 私信聊天页
├── LikesPage/                        # 点赞通知页
├── CommentsPage/                     # 评论通知页
├── FollowersPage/                    # 新粉丝页
├── NotificationsPage/                # 系统通知页
├── SharedComponents/                 # 共享组件
│   ├── ChatBubble/                   # 聊天气泡
│   ├── MessageItem/                  # 消息列表项
│   ├── SwipeActions/                 # 滑动操作
│   └── UserAvatar/                   # 用户头像
├── stores/                           # 状态管理
│   ├── messagesStore.ts              # 消息列表状态
│   ├── chatStore.ts                  # 聊天状态
│   └── webSocketStore.ts             # WebSocket状态
└── api/                              # API服务
    ├── messagesApi.ts                # 消息API
    ├── chatApi.ts                    # 聊天API
    └── notificationsApi.ts           # 通知API
```

**技术亮点**：
- WebSocket 实时消息推送
- 四宫格分类入口（点赞/评论/粉丝/通知）
- 模块内 Store 集中管理
- 扁平化区域组件架构

---

### 5. Profile 个人中心模块

**核心定位**：用户资料管理、订单查看、系统设置

**目录结构**：
```
Profile/
├── index.tsx                         # 模块入口
├── types.ts                          # 模块级类型
├── constants.ts                      # 模块级常量
├── MainPage/                         # 个人主页（四Tab）
│   ├── hooks/                        # 页面级Hooks
│   │   ├── useProfileData.ts         # 数据管理
│   │   ├── useProfileActions.ts      # 操作处理
│   │   └── useProfileNavigation.ts   # 导航管理
│   ├── UnifiedHeaderArea/            # 头部区域（嵌套）
│   │   ├── BackgroundLayer/          # 背景层
│   │   ├── TopActionBar/             # 顶部操作栏
│   │   └── UserInfoCard/             # 用户信息卡片
│   ├── TabNavigationArea/            # Tab导航
│   ├── TabContentArea/               # Tab内容（嵌套）
│   │   ├── DynamicContent/           # 动态内容
│   │   ├── CollectionContent/        # 收藏内容
│   │   ├── LikesContent/             # 点赞内容
│   │   └── ProfileContent/           # 资料内容
│   └── UnauthenticatedArea/          # 未登录状态
├── OtherUserProfilePage/             # 他人主页
├── ProfileEditPage/                  # 资料编辑页
├── MyOrdersPage/                     # 我的订单
├── MyPurchasesPage/                  # 我的购买
├── MySignupsPage/                    # 我的报名
├── SettingsPage/                     # 设置页面
├── CustomerServicePage/              # 客服页面
└── ExpertVerificationPage/           # 专家认证
```

**技术亮点**：
- 深度嵌套架构（3层以上UI层次）
- 四 Tab 框架（动态/收藏/点赞/资料）
- Hooks 职责分离（Data/Actions/Navigation）
- 订单状态区域组件化

---

### 6. Activity 组局活动模块

**核心定位**：线下活动发布、浏览、报名

**目录结构**：
```
Activity/
├── index.ts                          # 模块统一导出
├── types.ts                          # 类型定义
├── ActivityListPage/                 # 活动列表页
├── ActivityDetailPage/               # 活动详情页
├── PublishActivityPage/              # 发布活动页
└── FilterPage/                       # 活动筛选页
```

**技术亮点**：
- 简洁扁平化结构
- 完整的活动类型定义（探店/密室/私影/台球等）
- 筛选系统（排序/性别/人数/类型）

## 🔗 模块间依赖关系

```
┌─────────────────────────────────────────────────────────────────┐
│                        模块依赖关系图                             │
└─────────────────────────────────────────────────────────────────┘

                    ┌─────────────┐
                    │ AuthModule  │
                    │  (认证模块)  │
                    └──────┬──────┘
                           │ 提供认证状态
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ↓                  ↓                  ↓
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│  Homepage   │   │  Discovery  │   │  Messages   │
│  (首页模块)  │   │  (发现模块)  │   │  (消息模块)  │
└──────┬──────┘   └──────┬──────┘   └─────────────┘
       │                  │
       │                  ↓
       │          ┌─────────────┐
       │          │  Profile    │
       └─────────→│ (个人中心)  │
                  └──────┬──────┘
                         │
                         ↓
                  ┌─────────────┐
                  │  Activity   │
                  │ (组局活动)  │
                  └─────────────┘

依赖说明：
• AuthModule → 所有模块：提供 isAuthenticated 状态
• Homepage → Profile：用户详情跳转
• Discovery → Profile：动态作者跳转
• Profile → Activity：我的报名查看
```

## 🔄 数据流架构

### 整体数据流

```
┌─────────────────────────────────────────────────────────────────┐
│                         数据流架构                               │
└─────────────────────────────────────────────────────────────────┘

用户操作 (Touch/Input)
         │
         ↓
┌─────────────────────────────────────────────────────────────────┐
│                      UI 组件层                                   │
│  MainPage / DetailPage / *Area Components                       │
└─────────────────────────────┬───────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         │                    │                    │
         ↓                    ↓                    ↓
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│ Custom Hooks│      │ Zustand     │      │ React       │
│ useXxxData  │      │ Store       │      │ State       │
│ useXxxLogic │      │ useXxxStore │      │ useState    │
└──────┬──────┘      └──────┬──────┘      └─────────────┘
       │                    │
       └────────┬───────────┘
                │
                ↓
┌─────────────────────────────────────────────────────────────────┐
│                      API 服务层                                  │
│  xxxApi.ts (Axios HTTP Client)                                  │
└─────────────────────────────┬───────────────────────────────────┘
                              │
         ┌────────────────────┼────────────────────┐
         │                    │                    │
         ↓                    ↓                    ↓
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│ xypai-auth  │      │xypai-app-bff│      │ xypai-chat  │
│ (认证服务)   │      │ (BFF聚合)   │      │ (WebSocket) │
│ :8200       │      │ :9400       │      │ :9404       │
└─────────────┘      └──────┬──────┘      └─────────────┘
                            │
                            ↓
              ┌──────────────────────────┐
              │    领域服务层             │
              │ xypai-user / content /   │
              │ order / payment / common │
              └──────────────────────────┘
```

### 状态管理架构

```
┌─────────────────────────────────────────────────────────────────┐
│                      状态管理分层                                │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  全局状态 (Global State)                                        │
│  位置: stores/                                                  │
├─────────────────────────────────────────────────────────────────┤
│  • useUserStore      - 当前登录用户信息                          │
│  • useConfigStore    - 应用配置                                 │
│  • useLocationStore  - 位置信息                                 │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  模块状态 (Module State)                                        │
│  位置: features/{Module}/stores/                                │
├─────────────────────────────────────────────────────────────────┤
│  AuthModule:                                                    │
│  • authStore         - 认证主状态 (Token/User)                   │
│  • authFlowStore     - 认证流程状态                              │
│  • authDataStore     - 认证数据状态                              │
│  • authUIStore       - 认证UI状态                               │
│                                                                 │
│  Messages:                                                      │
│  • messagesStore     - 消息列表状态                              │
│  • chatStore         - 聊天状态                                 │
│  • webSocketStore    - WebSocket连接状态                        │
│                                                                 │
│  Discovery:                                                     │
│  • discoveryStore    - 发现页状态                               │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  页面状态 (Page State)                                          │
│  位置: features/{Module}/{Page}/hooks/                          │
├─────────────────────────────────────────────────────────────────┤
│  • useXxxData        - 页面数据获取                              │
│  • useXxxState       - 页面UI状态                               │
│  • useXxxActions     - 页面操作处理                              │
│  • useXxxNavigation  - 页面导航管理                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────────┐
│  组件状态 (Component State)                                     │
│  位置: 组件内部 useState / useReducer                            │
├─────────────────────────────────────────────────────────────────┤
│  • 表单输入状态                                                  │
│  • UI交互状态（展开/收起、动画等）                                │
│  • 临时缓存数据                                                  │
└─────────────────────────────────────────────────────────────────┘
```

## 📡 API 服务层

### 服务文件清单

```
services/api/
├── index.ts           # 统一导出入口
├── config.ts          # API 端点配置
├── client.ts          # HTTP 客户端基础封装
├── authApi.ts         # 认证服务 (xypai-auth)
├── bffApi.ts          # BFF 聚合服务 (xypai-app-bff)
├── commonApi.ts       # 通用服务 (xypai-common)
├── feedApi.ts         # 内容动态服务 (xypai-content)
├── filterApi.ts       # 首页筛选服务
├── relationApi.ts     # 用户关系服务 (xypai-user)
├── profileApi.ts      # 个人资料服务 (xypai-user)
├── discoveryApi.ts    # 发现页服务
├── homepageApi.ts     # 首页服务
├── locationApi.ts     # 位置服务
├── serviceApi.ts      # 服务类型配置
├── userApi.ts         # 用户服务
└── activityApi.ts     # 活动服务 (xypai-order)
```

### BFF 聚合服务 (bffApi.ts)

**核心功能**：首页用户推荐、限时专享、搜索功能、组局活动、筛选配置

| 方法 | 说明 | 后端接口 |
|------|------|---------|
| `getHomeFeed(params)` | 获取用户推荐列表 | `GET /xypai-app-bff/api/home/feed` |
| `getOnlineUsers(params)` | 获取线上用户（便捷方法） | - |
| `getOfflineUsers(params)` | 获取线下用户（便捷方法） | - |
| `getFilterConfig(type)` | 获取筛选配置 | `GET /xypai-app-bff/api/home/filter/config` |
| `applyFilter(params)` | 应用筛选条件 | `POST /xypai-app-bff/api/home/filter/apply` |
| `getLimitedTimeList(params)` | 获取限时专享列表 | `GET /xypai-app-bff/api/home/limited-time/list` |
| `getSearchInit()` | 获取搜索初始数据 | `GET /xypai-app-bff/api/search/init` |
| `getSearchSuggestions(keyword, limit)` | 获取搜索建议 | `GET /xypai-app-bff/api/search/suggest` |
| `deleteSearchHistory(params)` | 删除搜索历史 | `DELETE /xypai-app-bff/api/search/history` |
| `executeSearch(params)` | 执行综合搜索 | `POST /xypai-app-bff/api/search/search` |
| `getSearchAll(keyword, pageNum, pageSize)` | 获取全部Tab结果 | `GET /xypai-app-bff/api/search/all` |
| `getSearchUsers(keyword, pageNum, pageSize)` | 获取用户Tab结果 | `GET /xypai-app-bff/api/search/users` |
| `getSearchOrders(keyword, pageNum, pageSize)` | 获取下单Tab结果 | `GET /xypai-app-bff/api/search/orders` |
| `getSearchTopics(keyword, pageNum, pageSize)` | 获取话题Tab结果 | `GET /xypai-app-bff/api/search/topics` |
| `getActivityList(params)` | 获取活动列表 | `GET /xypai-app-bff/api/activity/list` |
| `getActivityDetail(activityId)` | 获取活动详情 | `GET /xypai-app-bff/api/activity/detail/{id}` |
| `registerActivity(params)` | 报名参加活动 | `POST /xypai-app-bff/api/activity/register` |
| `cancelActivityRegistration(params)` | 取消报名 | `POST /xypai-app-bff/api/activity/register/cancel` |

### 内容服务 (feedApi.ts)

**核心功能**：动态流、互动、评论、话题

| 方法 | 说明 | 后端接口 |
|------|------|---------|
| `getFeedList(tabType, params)` | 获取动态列表 | `GET /xypai-content/api/v1/content/feed/{tabType}` |
| `getFeedDetail(feedId)` | 获取动态详情 | `GET /xypai-content/api/v1/content/detail/{feedId}` |
| `publishFeed(params)` | 发布动态 | `POST /xypai-content/api/v1/content/publish` |
| `like(targetType, targetId, action)` | 点赞/取消 | `POST /xypai-content/api/v1/interaction/like` |
| `collect(targetType, targetId, action)` | 收藏/取消 | `POST /xypai-content/api/v1/interaction/collect` |
| `getHotTopics(page, pageSize)` | 获取热门话题 | `GET /xypai-content/api/v1/content/topics/hot` |
| `searchTopics(keyword, page, pageSize)` | 搜索话题 | `GET /xypai-content/api/v1/content/topics/search` |

### 通用服务 (commonApi.ts)

**核心功能**：媒体上传、位置服务

| 方法 | 说明 | 后端接口 |
|------|------|---------|
| `uploadImage(file, options, onProgress)` | 上传图片 | `POST /xypai-common/api/v1/media/upload` |
| `uploadVideo(file, onProgress)` | 上传视频 | `POST /xypai-common/api/v1/media/upload` |
| `getNearbyLocations(params)` | 获取附近地点 | `GET /xypai-common/api/v1/location/nearby` |
| `searchLocations(params)` | 搜索地点 | `GET /xypai-common/api/v1/location/search` |

### API 使用示例

```typescript
import { bffApi, feedApi, commonApi } from '@/services/api';

// 获取首页用户推荐
const users = await bffApi.getHomeFeed({ type: 'online', pageNum: 1 });

// 获取限时专享列表
const limitedTime = await bffApi.getLimitedTimeList({
  sortBy: 'smart',
  gender: 'all',
  pageSize: 10
});

// 获取热门话题
const topics = await feedApi.getHotTopics(1, 20);

// 上传图片
const result = await commonApi.uploadImage(file, { quality: 0.8 });
```

## 🎨 架构设计亮点

### 1. Features 模块化
- 按业务领域划分模块，每个模块完整独立
- 模块内包含：页面、组件、状态、API、类型
- 减少跨模块依赖，提高内聚性

### 2. 伪页面组件架构
- **扁平化模式**：简单组件直接在页面目录下
- **嵌套化模式**：复杂组件（UI≥3层）使用子目录
- **禁止 components/ 中间层**：区域组件直接位于页面下

### 3. Flow 业务流程组织
- Homepage 采用 Flow 模式（EventFlow, SearchFlow 等）
- 每个 Flow 包含完整的页面链路
- Flow 内可包含独立的 api/、types.ts

### 4. 八段式单文件架构
- 所有组件文件按 9 个 region 组织代码
- 标准顺序：Banner → Imports → Types → Constants → Utils → State → Logic → UI → Export
- 便于代码导航和维护

### 5. SharedComponents 复用
- 模块级共享组件放入 SharedComponents/
- 如：BottomSheets、ChatBubble、MessageItem
- 避免组件重复定义

### 6. Hooks 职责分离
- useXxxData：数据获取和缓存
- useXxxState：UI 状态管理
- useXxxActions：操作处理
- useXxxNavigation：路由导航

## 🚀 快速启动指南

### 1. 环境准备

```bash
# 确保已安装 Node.js 18+ 和 Expo CLI
npm install -g expo-cli

# 克隆项目
git clone <repository-url>
cd XiangYuPai-RNExpoAPP

# 安装依赖
npm install
```

### 2. 启动开发服务器

```bash
# 启动 Expo 开发服务器
npm start

# 或使用 Expo CLI
expo start

# 在 iOS 模拟器中运行
npm run ios

# 在 Android 模拟器中运行
npm run android
```

### 3. 后端服务依赖

```bash
# 需要启动的后端服务（按顺序）
1. xypai-user     (9401) - 用户服务
2. xypai-auth     (8200) - 认证服务
3. xypai-app-bff  (9400) - BFF聚合服务
4. xypai-content  (9403) - 内容服务
5. xypai-chat     (9404) - 聊天服务（WebSocket）
6. xypai-order    (9405) - 订单服务
7. xypai-payment  (9406) - 支付服务
```

### 4. 开发调试

```bash
# 内置测试账号（开发环境）
手机号: 13800138000
密码: test123456
验证码: 任意 6 位数字
```

## 📚 快速理解文档清单

| 模块 | 文档路径 | 说明 |
|------|---------|------|
| **AuthModule** | `src/features/AuthModule/快速理解.md` | 认证模块完整文档 |
| **Homepage** | `src/features/Homepage/快速理解.md` | 首页模块完整文档 |
| **Discovery** | `src/features/Discovery/快速理解.md` | 发现模块完整文档 |
| **Messages** | `src/features/Messages/快速理解.md` | 消息模块完整文档 |
| **Profile** | `src/features/Profile/快速理解.md` | 个人中心完整文档 |
| **Activity** | `src/features/Activity/快速理解.md` | 组局活动完整文档 |

## 📊 关键指标

### 代码量（估算）
- **总代码行数**：约 80,000 行
- **TypeScript 文件数**：约 400+ 个
- **组件数量**：约 150+ 个

### 性能指标
- **首屏加载**：< 3 秒
- **页面切换**：< 300ms
- **列表滚动**：60 FPS

### 支持平台
- **iOS**：14.0+
- **Android**：API 21+ (Android 5.0)

## 📌 开发规范

### 文件命名
- 页面组件：`PascalCase` + `Page` 后缀（如 `MainPage`）
- 区域组件：`PascalCase` + `Area` 后缀（如 `HeaderArea`）
- Hooks：`camelCase` + `use` 前缀（如 `useProfileData`）
- Store：`camelCase` + `Store` 后缀（如 `authStore`）
- API：`camelCase` + `Api` 后缀（如 `authApi`）

### 架构规范
- **禁止 components/ 中间层**：区域组件直接位于页面目录下
- **复杂组件嵌套**：UI 层次 ≥ 3 层时使用嵌套结构
- **模块内 Store**：复杂模块在 `stores/` 目录管理状态
- **模块内 API**：复杂模块在 `api/` 目录管理接口

### 代码规范
- ESLint + Prettier 自动格式化
- TypeScript 严格模式
- 组件使用 React.memo 优化
- Props 接口明确定义

## 🤝 团队协作

### 分支策略
- **main**：生产分支
- **develop**：开发分支
- **feature/xxx**：功能分支
- **hotfix/xxx**：紧急修复

### 提交规范
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式
refactor: 重构
test: 测试
chore: 构建/工具
```

---

**最后更新**：2025-11-28
**文档版本**：v1.1.0
**适用版本**：XiangYuPai-RNExpoAPP v1.0.0
