/**
 * 打印测试账号信息到控制台
 * 
 * 用于开发调试时快速查看可用的测试账号
 */

import { TEST_ACCOUNTS } from './testAccounts';

/**
 * 打印所有测试账号信息
 */
export const printTestAccounts = () => {
  console.log('\n╔═══════════════════════════════════════════════════════════╗');
  console.log('║              📱 可用测试账号列表                            ║');
  console.log('╚═══════════════════════════════════════════════════════════╝\n');

  TEST_ACCOUNTS.forEach((account, index) => {
    console.log(`${index + 1}️⃣  ${account.nickname}`);
    console.log(`   📱 手机号: ${account.phone}`);
    console.log(`   🔑 密码: ${account.password}`);
    console.log(`   💬 验证码: ${account.smsCode}`);
    console.log(`   ${account.verified ? '✅' : '❌'} 认证状态: ${account.verified ? '已认证' : '未认证'}`);
    if (account.description) {
      console.log(`   📝 说明: ${account.description}`);
    }
    console.log('');
  });

  console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');
  console.log('💡 提示: 只有上述测试账号可以登录，其他账号会被拒绝');
  console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n');
};

/**
 * 打印快速登录提示
 */
export const printQuickLoginHint = () => {
  console.log('\n🚀 快速登录提示:');
  console.log('   手机号: 13800138000');
  console.log('   密码: test123456');
  console.log('   验证码: 888888\n');
};

