# Homepage 首页模块快速理解

## 📌 核心定位
向娱拍 React Native App 的**首页模块**，提供用户列表浏览、服务筛选、搜索、组局等核心功能入口。

**技术栈**: React Native + Expo Router + TypeScript + Zustand

## 🎯 主要功能
- **用户列表展示**（瀑布流卡片布局）
- **多维度筛选**（区域/性别/价格/标签）
- **服务分类入口**（游戏陪玩/生活服务）
- **组局中心**（线下活动发布与参与）
- **搜索功能**（用户/话题/订单搜索）
- **位置选择**（城市/区域切换）
- **精选推荐**（限时优惠/特色服务）

## 🏗️ 技术栈
- React Native 0.72+
- Expo Router（路由导航）
- TypeScript（严格模式）
- Zustand（全局状态管理）
- 八段式单文件架构

## 📁 核心目录结构
```
Homepage/
├── index.ts                      # 模块统一导出入口
├── types.ts                      # 模块级共享类型
├── 快速理解.md                    # 本文档
│
├── MainPage/                     # 📱 首页主页面
│   ├── index.ts                  # 页面导出
│   ├── MainPage.tsx              # 主页面组件（八段式）
│   ├── types.ts                  # 页面类型定义
│   ├── constants.ts              # 页面常量配置
│   ├── useHomeState.ts           # 页面状态管理
│   ├── useHomeNavigation.ts      # 页面导航管理
│   ├── useHomeData.ts            # 页面数据管理
│   ├── homeApi.ts                # 首页API服务
│   │
│   ├── HeaderArea/               # 顶部导航区域
│   ├── GameBannerArea/           # 游戏横幅区域
│   ├── FunctionGridArea/         # 功能入口网格
│   ├── LimitedOffersArea/        # 限时专享区域
│   ├── TeamPartyArea/            # 组队聚会区域
│   ├── FilterTabsArea/           # 筛选标签栏（嵌套）
│   │   ├── RegionSelector/       # 地区选择器
│   │   └── FilterSelector/       # 筛选器
│   └── UserListArea/             # 用户列表区域（嵌套）
│       └── UserCardComponent/    # 用户卡片组件
│
├── SharedComponents/             # 🧩 模块内共享组件
│   └── BottomSheets/             # 底部弹窗组件
│       ├── SortBottomSheet.tsx   # 排序选择弹窗
│       ├── GenderBottomSheet.tsx # 性别筛选弹窗
│       └── AdvancedFilterSheet.tsx # 高级筛选弹窗
│
├── EventFlow/                    # 🎉 组局中心功能流
│   ├── EventCenterPage/          # 组局列表页
│   └── PublishEventPage/         # 发布组局页
│
├── SearchFlow/                   # 🔍 搜索功能流
│   ├── SearchMainPage/           # 搜索主页
│   ├── SearchResultsPage/        # 搜索结果页
│   └── api/                      # 搜索API服务
│
├── FilterFlow/                   # 🎛️ 筛选功能流
│   ├── FilterMainPage/           # 筛选主页
│   └── api/                      # 筛选API服务
│
├── ServiceFlow/                  # 🎮 服务详情功能流
│   ├── ServiceDetailPage/        # 📱 技能服务详情页（2025-12-04 重构）
│   │   ├── index.tsx             # 主页面组件 [L1]
│   │   ├── types.ts              # 类型定义（ServiceType/ServiceDetailData等）
│   │   ├── constants.ts          # 常量配置（颜色/尺寸/文案）
│   │   ├── NavArea.tsx           # 导航区域 [L2]
│   │   ├── UserInfoArea.tsx      # 用户信息区域 [L2]
│   │   ├── TagsArea.tsx          # 标签区域 [L2]（仅线上）
│   │   ├── SkillIntroArea.tsx    # 技能介绍 [L2]（仅线上）
│   │   ├── ActionBarArea.tsx     # 底部操作栏 [L2]
│   │   ├── GameCardArea/         # 游戏截图轮播 [L2]
│   │   │   └── index.tsx
│   │   ├── ActivityArea/         # 活动信息 [L2]（仅线下）
│   │   │   └── index.tsx
│   │   ├── ReviewArea/           # 评价区域 [L2]
│   │   │   └── index.tsx
│   │   └── hooks/
│   │       └── useServiceDetail.ts  # 数据管理Hook
│   ├── GamePlayerListPage/       # 📱 王者荣耀陪玩列表页（2025-12-03 新增）
│   │   ├── index.tsx             # 主页面组件
│   │   ├── types.ts              # 类型定义
│   │   ├── constants.ts          # 常量配置
│   │   └── PlayerCardItem.tsx    # 玩家卡片组件
│   ├── AllReviewsPage/           # 📱 全部评价页（待实现）
│   │   ├── index.tsx             # 主页面组件 [L1]
│   │   ├── types.ts              # 类型定义
│   │   ├── constants.ts          # 常量配置
│   │   ├── FilterTagsArea.tsx    # 筛选标签区 [L2]
│   │   └── ReviewListArea/       # 评价列表区 [L2]
│   │       ├── index.tsx
│   │       └── ReviewItem.tsx    # 评价项 [L3]
│   └── orderApi.ts               # 订单API服务
│
├── LocationFlow/                 # 📍 位置选择功能流
│   └── LocationMainPage/         # 位置选择页
│
├── FeaturedFlow/                 # ⭐ 精选推荐功能流
│   └── FeaturedPage/             # 精选页面
│
└── UserDetailFlow/               # 👤 用户详情功能流
    └── EnhancedUserDetailPage.tsx # 用户详情页
```

## 🗺️ 页面导航流程

### 页面清单

| 页面 | 路由 | 组件 | 状态 |
|------|------|------|------|
| **首页主页** | `/(tabs)/homepage` | `MainPage` | ✅ 已实现 |
| **搜索页** | `/(tabs)/homepage/search` | `SearchMainPage` | ✅ 已实现 |
| **位置选择** | `/(tabs)/homepage/location` | `LocationMainPage` | ✅ 已实现 |
| **服务详情(新)** | `/service/detail/[serviceId]` | `ServiceDetailPage` | ✅ 已实现（2025-12-04） |
| **服务详情(旧)** | `/(tabs)/homepage/service-detail` | `ServiceDetailPage` | ✅ 兼容保留 |
| **全部评价** | `/service/reviews/[serviceId]` | `AllReviewsPage` | 📝 待实现 |
| **王者陪玩列表** | `/(tabs)/homepage/game-player-list` | `GamePlayerListPage` | ✅ 已实现 |
| **组局中心** | `/(tabs)/homepage/event-center` | `EventCenterPage` | ✅ 已实现 |
| **发布组局** | `/(tabs)/homepage/publish-event` | `PublishEventPage` | ✅ 已实现 |
| **限时专享** | `/(tabs)/homepage/featured` | `FeaturedPage` | ✅ 已实现 |
| **线上筛选** | `/(tabs)/homepage/filter-online` | `FilterMainPage` | ✅ 已实现 |
| **用户详情** | `/profile/[userId]` | `OtherUserProfilePage` | ✅ 已实现 |
| **技能详情** | `/skill/[skillId]` | `SkillDetailPage` | ✅ 已实现 |
| **下单页面** | `/order/create` | `OrderCreatePage` | ✅ 已实现 |

### 页面跳转关系图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Homepage 模块导航流程                               │
└─────────────────────────────────────────────────────────────────────────────┘

                              ┌──────────────────┐
                              │  /(tabs)/homepage │
                              │    MainPage       │
                              │    首页主页面      │
                              └────────┬─────────┘
                                       │
       ┌───────────┬───────────┬───────┼───────┬───────────┬───────────┐
       │           │           │       │       │           │           │
       ↓           ↓           ↓       ↓       ↓           ↓           ↓
┌──────────┐ ┌──────────┐ ┌──────────┐ │ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ 位置选择  │ │ 搜索页面  │ │ 服务详情  │ │ │ 组局中心  │ │ 限时专享  │ │ 发布组局  │
│ location │ │ search   │ │ service- │ │ │ event-   │ │ featured │ │ /publish │
│          │ │          │ │ detail   │ │ │ center   │ │          │ │          │
└────┬─────┘ └────┬─────┘ └────┬─────┘ │ └────┬─────┘ └────┬─────┘ └────┬─────┘
     │            │            │       │      │            │            │
     │            │            │       │      │            │            │
     ↓            ↓            ↓       ↓      ↓            ↓            ↓
┌──────────────────────────────────────────────────────────────────────────────┐
│                              用户详情页                                       │
│                         /profile/[userId]                                    │
│                       OtherUserProfilePage                                   │
└─────────────────────────────────┬────────────────────────────────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    ↓                           ↓
             ┌──────────────┐           ┌──────────────┐
             │ 技能详情页    │           │ 聊天页面      │
             │/skill/[id]   │           │/messages/chat│
             └──────┬───────┘           └──────────────┘
                    │
                    ↓
             ┌──────────────┐
             │ 下单页面      │
             │/order/create │
             └──────────────┘
```

### 详细导航说明

#### 1. 首页主页 (MainPage) 出发的导航

| 触发区域 | 触发操作 | 目标页面 | 导航代码 |
|---------|---------|---------|---------|
| HeaderArea | 点击位置 | 位置选择页 | `router.push('/(tabs)/homepage/location')` |
| HeaderArea | 点击搜索 | 搜索页 | `router.push('/(tabs)/homepage/search')` |
| GameBannerArea | 点击横幅 | 服务详情页 | `router.push({ pathname: '/(tabs)/homepage/service-detail', params: { serviceType } })` |
| FunctionGridArea | 点击功能入口 | 服务详情页 | `router.push({ pathname: '/(tabs)/homepage/service-detail', params: { serviceType } })` |
| TeamPartyArea | 点击更多 | 组局中心 | `router.push('/(tabs)/homepage/event-center')` |
| TeamPartyArea | 点击组局卡片 | 组局中心 | `router.push('/(tabs)/homepage/event-center')` |
| LimitedOffersArea | 点击更多 | 限时专享 | `router.push('/(tabs)/homepage/featured')` |
| LimitedOffersArea | 点击用户卡片 | 用户详情 | `router.push({ pathname: '/profile/[userId]', params: { userId } })` |
| UserListArea | 点击用户卡片 | 用户详情 | `router.push({ pathname: '/profile/[userId]', params: { userId } })` |
| FAB按钮 | 点击发布 | 发布组局 | `router.push('/publish')` |
| 底部Tab | 点击发现 | 发现页 | `router.push('/(tabs)/discover')` |

#### 2. 搜索页 (SearchMainPage) 出发的导航

| 触发操作 | 目标页面 | 导航代码 |
|---------|---------|---------|
| 搜索用户结果点击 | 用户详情 | `router.push({ pathname: '/profile/[userId]', params: { userId } })` |
| 搜索技能结果点击 | 技能详情 | `router.push({ pathname: '/skill/[skillId]', params: { skillId } })` |
| 搜索话题结果点击 | 话题详情 | `router.push({ pathname: '/topic/[topicId]', params: { topicId } })` |

#### 3. 王者陪玩列表页 (GamePlayerListPage) 出发的导航

| 触发操作 | 目标页面 | 导航代码 |
|---------|---------|---------|
| 点击玩家卡片 | 服务详情页 | `router.push({ pathname: '/service/detail/[serviceId]', params: { serviceId, serviceType: 'online' } })` |
| 点击返回 | 上一页 | `router.back()` |

#### 4. 服务详情页 (ServiceDetailPage) 出发的导航

| 触发操作 | 目标页面 | 导航代码 |
|---------|---------|---------|
| 点击头像 | 用户详情 | `router.push({ pathname: '/profile/[userId]', params: { userId } })` |
| 点击评价用户 | 用户详情 | `router.push({ pathname: '/profile/[userId]', params: { userId } })` |
| 点击查看全部评价 | 评价列表 | `router.push({ pathname: '/service/reviews/[serviceId]', params: { serviceId } })` |
| 点击私信 | 功能开发中提示 | `Alert.alert('功能开发中')` |
| 点击下单 | 订单确认页 | `router.push({ pathname: '/order/confirm', params: { serviceId } })` |
| 点击返回 | 上一页 | `router.back()` |

#### 5. 组局中心 (EventCenterPage) 出发的导航

| 触发操作 | 目标页面 | 导航代码 |
|---------|---------|---------|
| 点击组局卡片 | 技能详情 | `router.push('/skill/${eventId}?contentType=event')` |
| 点击发布按钮 | 发布组局 | `router.push('/(tabs)/homepage/publish-event')` |

#### 5. 限时专享 (FeaturedPage) 出发的导航

| 触发操作 | 目标页面 | 导航代码 |
|---------|---------|---------|
| 点击用户卡片 | 用户详情 | `router.push({ pathname: '/profile/[userId]', params: { userId } })` |

### 跨模块导航（从 Homepage 出发）

| 目标模块 | 目标页面 | 触发场景 | 路由 |
|---------|---------|---------|------|
| **Profile** | 用户详情 | 点击任意用户卡片 | `/profile/[userId]` |
| **Profile** | 技能详情 | 点击技能/组局卡片 | `/skill/[skillId]` |
| **Discovery** | 发现页 | Tab切换 | `/(tabs)/discover` |
| **Messages** | 聊天页 | 用户详情页点击私信 | `/(tabs)/messages/chat/[conversationId]` |
| **Activity** | 活动列表 | 首页点击活动入口 | `/activity` |
| **Order** | 下单页 | 用户详情点击下单 | `/order/create` |

## 🔑 核心接口

### 页面组件
| 组件 | 说明 | 路由 |
|------|------|------|
| `MainPage` | 首页主页面 | `/(tabs)/homepage` |
| `ServiceDetailPage` | 服务详情页 | `/(tabs)/homepage/service-detail` |
| `EventCenterPage` | 组局中心 | `/(tabs)/homepage/event-center` |
| `SearchMainPage` | 搜索页 | `/(tabs)/homepage/search` |
| `FilterMainPage` | 筛选页 | `/(tabs)/homepage/filter-online` |
| `LocationMainPage` | 位置选择 | `/(tabs)/homepage/location` |
| `FeaturedPage` | 限时专享 | `/(tabs)/homepage/featured` |
| `PublishEventPage` | 发布组局 | `/(tabs)/homepage/publish-event` |

### API 服务 (BFF 聚合)

**文件位置**: `services/api/bffApi.ts`

| 方法 | 说明 | 后端接口 |
|------|------|---------|
| `getHomeFeed(params)` | 获取用户推荐列表 | `GET /xypai-app-bff/api/home/feed` |
| `getOnlineUsers(params)` | 获取线上用户 | 便捷方法 |
| `getOfflineUsers(params)` | 获取线下用户 | 便捷方法 |
| `getFilterConfig(type)` | 获取筛选配置 | `GET /xypai-app-bff/api/home/filter/config` |
| `applyFilter(params)` | 应用筛选条件 | `POST /xypai-app-bff/api/home/filter/apply` |
| `getLimitedTimeList(params)` | 获取限时专享列表 | `GET /xypai-app-bff/api/home/limited-time/list` |
| `getSearchInit()` | 获取搜索初始数据 | `GET /xypai-app-bff/api/search/init` |
| `getSearchSuggestions(keyword, limit)` | 获取搜索建议 | `GET /xypai-app-bff/api/search/suggest` |
| `deleteSearchHistory(params)` | 删除搜索历史 | `DELETE /xypai-app-bff/api/search/history` |
| `executeSearch(params)` | 执行综合搜索 | `POST /xypai-app-bff/api/search/search` |
| `getSearchAll(keyword, pageNum, pageSize)` | 获取全部Tab结果 | `GET /xypai-app-bff/api/search/all` |
| `getSearchUsers(keyword, pageNum, pageSize)` | 获取用户Tab结果 | `GET /xypai-app-bff/api/search/users` |
| `getSearchOrders(keyword, pageNum, pageSize)` | 获取下单Tab结果 | `GET /xypai-app-bff/api/search/orders` |
| `getSearchTopics(keyword, pageNum, pageSize)` | 获取话题Tab结果 | `GET /xypai-app-bff/api/search/topics` |
| `getActivityList(params)` | 获取活动列表 | `GET /xypai-app-bff/api/activity/list` |
| `getActivityDetail(activityId)` | 获取活动详情 | `GET /xypai-app-bff/api/activity/detail/{id}` |
| `registerActivity(params)` | 报名参加活动 | `POST /xypai-app-bff/api/activity/register` |
| `cancelActivityRegistration(params)` | 取消报名 | `POST /xypai-app-bff/api/activity/register/cancel` |
| `getServiceDetail(params)` | 获取服务详情 | `GET /xypai-app-bff/api/service/detail` |
| `getServiceReviews(params)` | 获取服务评价列表 | `GET /xypai-app-bff/api/service/reviews` |

**服务详情 API 参数**:
```typescript
interface ServiceDetailParams {
  serviceId: number;     // 服务ID
  userId?: string;       // 用户ID（可选，用于个性化推荐）
}

interface ServiceReviewsParams {
  serviceId: number;     // 服务ID
  pageNum?: number;      // 页码，默认1
  pageSize?: number;     // 每页数量，默认10
  filterBy?: 'all' | 'excellent' | 'good' | 'negative' | 'with_images';
}
```

**限时专享 API 参数**:
```typescript
interface LimitedTimeQueryParams {
  pageNum?: number;      // 页码，默认1
  pageSize?: number;     // 每页数量，默认10
  sortBy?: 'smart' | 'price_asc' | 'price_desc' | 'distance_asc';  // 排序方式
  gender?: 'all' | 'male' | 'female';  // 性别筛选
  language?: string;     // 语言筛选（如"普通话"、"粤语"）
}
```

**活动列表 API 参数**:
```typescript
interface ActivityListParams {
  pageNum?: number;      // 页码，默认1
  pageSize?: number;     // 每页数量，默认10
  sortBy?: 'smart_recommend' | 'newest' | 'distance';  // 排序方式
  gender?: 'all' | 'male' | 'female';  // 性别筛选
  memberCount?: string;  // 人数筛选，如"2-4"、"5-8"
  activityType?: string; // 活动类型，如"billiards"、"ktv"
  cityCode?: string;     // 城市代码
  districtCode?: string; // 区域代码
}
```

**API 使用示例**:
```typescript
import { bffApi } from '@/services/api';

// 获取用户推荐
const users = await bffApi.getHomeFeed({ type: 'online', pageNum: 1 });

// 获取限时专享列表
const limitedTime = await bffApi.getLimitedTimeList({
  sortBy: 'smart',
  gender: 'all',
  pageSize: 10
});

// 获取活动列表
const activities = await bffApi.getActivityList({
  sortBy: 'smart_recommend',
  gender: 'all',
  activityType: 'billiards',
  pageSize: 10
});

// 获取活动详情
const detail = await bffApi.getActivityDetail(12345);

// 报名参加活动
const result = await bffApi.registerActivity({
  activityId: 12345,
  message: '我想参加！'
});
```

### 状态管理 Hooks
| Hook | 说明 |
|------|------|
| `useHomeState()` | 首页UI状态（loading/error/tab） |
| `useHomeData()` | 首页数据（用户列表/筛选结果） |
| `useHomeNavigation()` | 首页导航（跳转/返回） |

### 共享组件
| 组件 | 说明 |
|------|------|
| `SortBottomSheet` | 排序选择弹窗 |
| `GenderBottomSheet` | 性别筛选弹窗 |
| `AdvancedFilterSheet` | 高级筛选弹窗 |

## 🏛️ 架构设计

### 模块架构图
```
┌──────────────────────────────────────────────────────┐
│                    Homepage 模块                      │
│                   (index.ts 统一导出)                  │
└─────────────────────────┬────────────────────────────┘
                          │
    ┌─────────────────────┼─────────────────────┐
    │                     │                     │
    ↓                     ↓                     ↓
┌─────────┐      ┌───────────────┐      ┌─────────────┐
│MainPage │      │ SharedComponents│      │ *Flow 页面  │
│(首页)    │      │ (共享组件)     │      │ (功能流)    │
└────┬────┘      └───────────────┘      └──────┬──────┘
     │                                         │
┌────┴──────────────────────────────┐   ┌─────┴─────┐
│          MainPage 区域组件         │   │EventFlow  │
├───────────────────────────────────┤   │SearchFlow │
│ HeaderArea      │ GameBannerArea  │   │FilterFlow │
│ FunctionGridArea│ LimitedOffers   │   │ServiceFlow│
│ TeamPartyArea   │ FilterTabsArea  │   │LocationFlow│
│ UserListArea    │                 │   │FeaturedFlow│
└─────────────────┴─────────────────┘   │UserDetail │
                                        └───────────┘
```

### 数据流向
```
用户操作
    ↓
MainPage/useHomeNavigation (导航分发)
    ↓
┌───────────────────────────────────┐
│     *Flow 功能页面                 │
│  (SearchFlow/FilterFlow/...)      │
└─────────────────┬─────────────────┘
                  ↓
              Flow API
                  ↓
          后端服务 (xypai-*)
                  ↓
           数据返回/缓存
                  ↓
         UI 更新/状态同步
```

## 🔥 技术亮点

1. **Flow 模式组织**：按业务流程划分子模块（EventFlow/SearchFlow 等），每个 Flow 完整独立
2. **嵌套化复杂组件**：FilterTabsArea、UserListArea 采用嵌套架构，支持复杂交互
3. **SharedComponents 复用**：底部弹窗组件统一管理，EventFlow 和 ServiceFlow 共享
4. **八段式结构**：所有页面和区域组件遵循八段式代码组织
5. **状态 Hooks 分离**：State/Data/Navigation 三个 Hook 职责分离
6. **扁平化区域组件**：简单区域直接位于 MainPage/ 下，无中间层

## 🚀 快速启动

### 导入方式
```typescript
// 导入主页面
import { MainPage } from '@/src/features/Homepage';

// 导入功能页面
import {
  SearchMainPage,
  ServiceDetailPage,
  EventCenterPage
} from '@/src/features/Homepage';

// 导入共享组件
import {
  SortBottomSheet,
  GenderBottomSheet
} from '@/src/features/Homepage';

// 导入类型
import type {
  UserCardInfo,
  ServiceType,
  FilterState
} from '@/src/features/Homepage/types';
```

### 路由配置
```
app/
├── (tabs)/
│   └── homepage/
│       ├── index.tsx             # MainPage 入口
│       ├── service-detail.tsx    # ServiceDetailPage（旧路由）
│       └── game-player-list.tsx  # GamePlayerListPage
├── service/
│   └── detail/
│       └── [serviceId].tsx       # ServiceDetailPage（新路由）
├── event-center.tsx              # EventCenterPage
├── search.tsx                    # SearchMainPage
└── filter.tsx                    # FilterMainPage
```

### ServiceDetailPage 路由参数
```typescript
// 路由: /service/detail/[serviceId]
interface ServiceDetailRouteParams {
  serviceId: string;      // 服务ID
  serviceType: 'online' | 'offline';  // 服务类型
}

// 使用示例
router.push({
  pathname: '/service/detail/[serviceId]',
  params: { serviceId: '123', serviceType: 'online' }
});
```

## 📌 注意事项

### 架构规范
- **禁止 components/ 中间层**：区域组件直接位于页面目录下
- **Flow 内部独立**：每个 Flow 可包含自己的 api/、types.ts
- **共享组件**：多 Flow 共用的组件放入 SharedComponents/
- **嵌套组件**：复杂组件（UI≥3层）使用嵌套结构

### 状态管理
- **页面级 Hooks**：useHomeState/Data/Navigation 管理首页状态
- **全局 Store**：使用 Zustand useUserStore、useConfigStore
- **API 缓存**：各 Flow 的 api/ 目录包含 Mock 数据和真实 API

### 导航约定
- **服务详情**：通过 `serviceType` 参数区分服务类型
- **搜索结果**：通过 `keyword` 和 `type` 参数传递
- **筛选条件**：通过全局状态或路由参数传递

### 组件复用
- **BottomSheets**：排序/性别/高级筛选弹窗已统一到 SharedComponents
- **区域组件**：HeaderArea 等可在其他页面复用
- **UserCard**：用户卡片组件可在搜索结果等页面复用

### 开发调试
- **Mock/真实 API 切换**：bffApi.ts 中 `USE_MOCK_DATA` 标志控制数据来源
- **数据转换层**：useServiceDetail.ts 包含 bffApi → 组件数据模型的转换函数
- **日志输出**：API 调用有详细 console.log 日志
- **错误边界**：页面使用 ErrorBoundary 包裹

### 性能优化
- **FlatList**：用户列表使用虚拟列表
- **图片懒加载**：用户头像/服务图片按需加载
- **组件 memo**：区域组件使用 React.memo

---

**最后更新**: 2025-12-04（新增 AllReviewsPage 全部评价页结构）
